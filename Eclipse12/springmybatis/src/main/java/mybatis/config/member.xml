<?xml version="1.0" encoding="UTF-8"?>
<!-- Sql Mapper -->
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<select id="findMemberId" parameterType="int" resultType="member">
		<![CDATA[
		SELECT	id, password, name, address
		FROM	member_new
		WHERE	id=#{id}
		]]>
	</select>
	
	<select id="findmMember" resultType="member">
		SELECT id, password, name, address 
		FROM member_new
	</select>
	
	<select id="findMemberById2" parameterType="string" resultType="member">
		SELECT id, password, name, address 
		FROM member_new 
		WHERE address=#{address}
	</select>
	
	<select id="findMemberByIdAndPwd" parameterType="selectionconditions" resultType="member">
		SELECT id, password, name, address 
		FROM member_new WHERE id=#{id} 
		AND password=#{password}
	</select>

	<insert id="insertMember" parameterType="member">
		<![CDATA[
		INSERT INTO member_new 
		VALUES(#{id}, #{password}, #{name}, #{address})
		]]>
	</insert>

	<update id="updateMemberById" parameterType="updateconditions">
		UPDATE member_new SET address=#{address} WHERE id=#{id}
	</update>

</mapper>